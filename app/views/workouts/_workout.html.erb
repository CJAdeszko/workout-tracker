<div id="<%= dom_id workout %>">
  <%= link_to(workout) do %>
    <div class="flex flex-row">
      <div class="basis-1/3 md:basis-1/8"><%= workout.created_at.strftime("%m/%d/%y") %></div>
      <div class="basis-2/3 md:basis-7/8"><%= workout.name %></div>
    </div>
  <% end%>
</div>
